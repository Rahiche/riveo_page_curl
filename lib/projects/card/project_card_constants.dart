class ProjectCardConstants {
  static const double cornerRadius = 16.0;
  static const Duration animationDuration = Duration(milliseconds: 500);
  static const Duration pauseAnimationDuration = Duration(milliseconds: 1000);
  static const Duration deleteAnimationDuration = Duration(milliseconds: 500);
  static const Duration seekAudioDuration = Duration(milliseconds: 350);
  static const String audioAsset = 'peel.mp3';
  static const String shaderAssetKey = 'shaders/page_curl.frag';
  static const double dialogThreshold = -30;
}
